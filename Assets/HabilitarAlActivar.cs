using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.EventSystems;

public class HabilitarAlActivar : MonoBehaviour
{
    /*GameObject primerObjeto;
    private void OnEnable()
    {
        primerObjeto = transform.GetChild(0).gameObject;
        if (EventSystem.current.currentSelectedGameObject == primerObjeto && primerObjeto.activeSelf)
        {
            primerObjeto.SetActive(false);
            primerObjeto.SetActive(true);
            primerObjeto.GetComponent<Button>().Select();
        }
        else
        {
            primerObjeto.GetComponent<Button>().Select();
        }
    }
*/
    
}
